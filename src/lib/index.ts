// place files you want to import through the `$lib` alias in this folder.

// 型定義のエクスポート
export { DEFAULT_LOCALE, LANGUAGE_NAMES, SUPPORTED_LOCALES } from './types/locale.js';
export type { Locale } from './types/locale.js';
